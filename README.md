# RADII
Custom-trained offline wake-word detection ("PILOT") on ESP32 with INT8 TensorFlow Lite Micro, handing off to cloud ASR.
This model is trained to detect a custom wake word "PILOT" in order to wake the system UP, for any task execution or else.
